# Security

- Do not paste live Ashby API keys into issue threads or logs.
- Prefer `ASHBY_API_KEY` or `ashby auth set --stdin`.
- Never print full tokens.
- Treat candidate data as sensitive.
- Do not broaden mutation coverage without documenting the risk surface first.

