# Contributing

## Local development

```bash
npm install
npm run lint
npm test
```

## Principles

- Keep the CLI official-API-first
- Prefer small explicit commands over broad abstractions
- Keep stdout machine-readable in `--json` mode
- Keep mutations narrow and unsurprising

